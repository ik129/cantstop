// TODO: Firebase Consoleから実際のFirebaseプロジェクト設定をコピーして貼り付けてください。
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
  measurementId: "YOUR_MEASUREMENT_ID" // 任意
};

// Firebaseを初期化する
// const app = firebase.initializeApp(firebaseConfig);
// const db = firebase.firestore(); // Firestoreを使用する場合

// 注意: 上記のfirebase.initializeAppやfirebase.firestore()を有効にするには、
// HTMLファイルでFirebase SDKを読み込む必要があります。
// 例:
// <script src="https://www.gstatic.com/firebasejs/8.10.0/firebase-app.js"></script>
// <script src="https://www.gstatic.com/firebasejs/8.10.0/firebase-firestore.js"></script>
// (バージョンは適宜最新のものに更新してください)
